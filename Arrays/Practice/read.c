#include<stdio.h>
int main()
{
    int i,t,a[10];
    printf("Enter the elements of array:");
    for(i=0;i<10;i++)
    scanf("%d",&a[i]);
    a[i]=t;
    for(i=0;i<10;i++)
    {
        printf("The numbers are:%d\n",a[i]);
    }
    return 0;
}